# OpenZeppelin-shaped runtime SDK backlog

> Status: living implementation inventory. This is a compatibility-shaped SDK plan, not a claim
> that ProofForge is a Solidity/OpenZeppelin replacement.
>
> **Authority snapshot:** `contracts/` from
> [`OpenZeppelin/openzeppelin-contracts` master tree
> `641ba990cad2f7f70878e0d66be1bfbef95710e8`](https://api.github.com/repos/OpenZeppelin/openzeppelin-contracts/git/trees/641ba990cad2f7f70878e0d66be1bfbef95710e8),
> fetched on 2026-09-03 with
> `gh api 'repos/OpenZeppelin/openzeppelin-contracts/git/trees/master?recursive=1'`.
> It contained 452 `contracts/` tree paths (367 Solidity sources), spanning `access`, `account`,
> `crosschain`, `finance`, `governance`, `interfaces`, `metatx`, `mocks`, `proxy`, `token`,
> `utils`, `vendor`, documentation, and package metadata.
>
> Status labels describe the fail-closed ProofForge product boundary, not source-code similarity:
> **DONE** means the bounded runtime capability is shipped; **PARTIAL** means a named restricted
> profile exists; **ABSENT** means no corresponding profile is shipped.

## Coverage

| OZ path | Status | ProofForge module / example | Gap relative to OZ | Implementable? (blocker) |
|---|---|---|---|---|
| `access/Ownable.sol`, `Ownable2Step.sol` | PARTIAL | `Sdk.Access`, `Sdk.Ownable`, `TwoStepCounter`, `Credits` | TwoStepCounter/Credits CREATE reverts `OwnableInvalidOwner(address)` and logs `OwnershipTransferred(address(0), owner)`; nominate-zero still `ZeroAddress()`; VestLink/Vest20 emit the same CREATE revert and log | Start event: W3; `renounceOwnership`: W5 slice 5; constructor OwnershipTransferred log: yes (VestLink/Vest20/TwoStepCounter/Credits); revert-only CREATE OwnableInvalidOwner prefix: yes (VestLink/Vest20/TwoStepCounter/Credits) |
| `access/AccessControl.sol`, `IAccessControl.sol` | PARTIAL | `Sdk.Roles.Set2`/`Set4`, `EvmStaticCounter`, `EvmStaticRoster` | Set2/Set4 fixed capacity, no role admin hierarchy/enumeration | `RoleAdminChanged`: no (no role-admin state/API); unbounded named roles: no |
| `access/extensions/*`, `access/manager/*` | PARTIAL | `Sdk.DefaultAdminDelay`, `AdminDelayLink` | Bounded delayed default-admin only; no role enumeration or authority manager | Yes — W5 slice e shipped bounded delayed default-admin profile; enumeration/manager remain blocked |
| `account/**` | ABSENT | — | ERC-4337/7579 account, paymaster, execution modes | No — AA/paymaster and arbitrary execution are product non-goals |
| `crosschain/**` | ABSENT | — | Bridge state, remote executor, receiver callbacks | No — cross-chain authentication/callback model is absent |
| `finance/VestingWallet*.sol` | PARTIAL | `Sdk.Vesting`, `VestLink`, `Vest20Link`, `Context.timestamp` | Named restricted profile: VestLink is ETH-only; Vest20Link is dual-asset | No remaining implementable slice. Dual-asset already lives on Vest20Link. VestLink stays the smaller ETH-only example. Row stays PARTIAL. W4 slice 3 shipped native-ETH `release(uint256)`; map-backed `Vest20Link.release(address)`; stored-beneficiary `transferOwnership` and parameterless `VestLink.release()`; constructor-stored OZ cliff (`cliffDuration`, `cliff()`); CREATE `OwnableInvalidOwner(address)`; constructor `OwnershipTransferred(address(0), owner)` log; only-owner `OwnableUnauthorizedAccount(address)`; `Vest20Link` dual-asset `release()` plus `release(address)`; ABI `released()` / `released(address)`; Ownable2Step `transferOwnership` / `acceptOwnership` / `pendingOwner` (`temporaryGapCount` 0) |
| `governance/**` | ABSENT | `Sdk.StorageCheckpoints` only | Proposal/vote/quorum/timelock lifecycle | No — Governor requires unconstrained proposal/voter relations and token snapshot semantics |
| `interfaces/IERC165.sol` | DONE | `Sdk.Erc165`, `Collectible`, `Badge`, `MultiToken`, `CraftToken` | Static, explicitly declared `IERC165` only; incomplete token interfaces return false | Yes — W1 shipped |
| `interfaces/IERC20*.sol`, `IERC2612.sol` | PARTIAL | `Sdk.Fungible`, `Sdk.Erc20Meta`, `Sdk.SafeErc20`, `Erc20Meta`, `Token`, `SafePay`, `Sdk.Payments` | Profiles, not a complete audited token; `Erc20Meta` ships issuer `permit` / `DOMAIN_SEPARATOR` / `nonces` over the closed Token/1 domain; no extensions/permit-votes ecosystem; mint gap closed on `Erc20Meta` (owner-gated) | Yes — W5 slice 4 shipped bounded string metadata SDK + typed canonical Transfer/Approval on `Erc20Meta`; W5 slice 6 closed owner-gated mint on `Erc20Meta`; this expansion shipped issuer permit on `Erc20Meta`; extensions/permit-votes remain blocked |
| `interfaces/IERC721*.sol`, `IERC4906.sol`, `IERC2309.sol` | PARTIAL | `Sdk.Erc721`, `Sdk.Erc721.Enum`, `Sdk.MetadataUri`, `Collectible`, `Badge`, `Gallery`, `ArtLink`, `ReceiverLink`, `Sdk.Erc165` | `safeTransferFrom(address,address,uint256,bytes)` with the outbound `onERC721Received` check shipped on `Collectible` (`Erc721.checkOnReceived` over `OpenCall.callMagic`; `data` at most 32 bytes). The three-argument overload is `Collectible.safeTransferFrom__id` (ABI `safeTransferFrom(address,address,uint256)`, empty `data`; Collectible is 10,879 runtime bytes after both overloads). Bounded receiving side shipped on `ReceiverLink` (`onERC721Received` returns `0x150b7a02` as `bytes4`; `data` at most 32 bytes; 2,145 runtime bytes); bounded IERC721Enumerable shipped on `Gallery` (`totalSupply`, `tokenByIndex`, `tokenOfOwnerByIndex`; UInt64 ids; compile-time capacity 4; owner-only `transferFrom`/`burn`; OOB views return 0 rather than OZ `ERC721OutOfBoundsIndex`); bounded metadata URI shipped | Outbound receiver check: Phase 3 (shipped); 3-arg overload: shipped; receiving side: ReceiverLink; bounded enumerable: this expansion; unbounded enumerable map: no |
| `interfaces/IERC1155*.sol` | PARTIAL | `Sdk.Erc1155`, `Sdk.MetadataUri`, `MultiToken`, `CraftToken`, `PackLink`, `ReceiverLink`, `Sdk.Erc165` | `safeTransferFrom(address,address,uint256,uint256,bytes)` with the outbound `onERC1155Received` check shipped on `CraftToken` and `MultiToken` (`Erc1155.checkOnReceived` over `OpenCall.callMagic`; `data` at most 32 bytes). `safeBatchTransferFrom(address,address,uint256[],uint256[],bytes)` with the outbound `onERC1155BatchReceived` check shipped on `MultiToken` (`Erc1155.checkOnBatchReceived` over two `BoundedVec UInt256 4` OpenCall args plus `BoundedBytes 32`; selector `0xbc197c81`; `MultiToken` is 18,925 runtime bytes, 5,651 under EIP-170, after the pair-map load helpers `pf_load_pair256` / `pf_load_pair_u64`). Duplicate ids revert `DuplicateId()` rather than OZ applying slots in order. No unbounded arrays. Bounded receiving side shipped on `ReceiverLink` (`onERC1155Received` returns `0xf23a6e61`, `onERC1155BatchReceived` returns `0xbc197c81`, both as `bytes4`; `data` at most 32 bytes; batch at most four slots, recorded id/value is slot 0; 2,145 runtime bytes). Bounded `balanceOfBatch` (four slots, OZ `ERC1155InvalidArrayLength` and `ERC1155InsufficientBalance` reverts, OZ log rule: one `TransferBatch` over the submitted slots, `TransferSingle` for a one-slot batch) and bounded metadata URI shipped | bounded URI: W4; bounded `balanceOfBatch`, `safeBatchTransferFrom`, and `TransferBatch`: Phase 2; outbound single and batch receiver checks: Phase 3 (shipped); receiving side: ReceiverLink (this expansion) |
| `interfaces/IERC2981.sol` | DONE | `Sdk.Erc2981`, `RoyaltyArt`, `Sdk.Erc165` | Static receiver + basis-point quote; `tokenId` ignored | Yes — W2 shipped |
| `interfaces/IERC1271.sol` | PARTIAL | `Sdk.Ierc1271.checkNow` (OZ `isValidSignatureNow`), `checkSignature` (`isValidSignature(bytes32,bytes)` over `OpenCall.callMagic`, magic `0x1626ba7e`), `SignerLink.requireNow` / `requireSigner`, `Sdk.Ecdsa` | The signature is one ECDSA `r ‖ s ‖ v`, a `BoundedBytes 65` (the packed `bytes` ceiling); a wider multi-signature wallet payload is out. Both helpers revert, where OZ answers `false`. `checkSignature` has no ECDSA fallback. `checkNow` is the code-size branch: code takes `checkSignature`, no code recovers. The receiving side, `isValidSignature` on a `pf` contract, is a non-goal | Phase 3 shipped `checkSignature`. This expansion shipped `checkNow`. `Sdk.OzAudit` counts the row PARTIAL and `temporaryGapCount` is 0. Remaining: receiving side, wider payloads, `false` vs revert |
| `interfaces/IERC3156*.sol`, `IERC4626.sol`, `IERC6909.sol`, `IERC777*.sol`, `IERC1363*.sol` | PARTIAL | `Sdk.Erc4626`, `Sdk.Erc6909`, `Vault4626Link`, `NineLink` | Flash callbacks, hooks, dynamic exchange-rate vault math, broad multi-id operator model | Yes — W5 slice 7 shipped bounded 1:1 ERC-4626 vault profile; W5 slice 8 shipped bounded fixed-id ERC-6909; flash/777/1363 remain blocked |
| `interfaces/IERC1820*.sol`, `IERC1967.sol`, `IERC1822.sol` | ABSENT | — | Global registry/proxy-slot interoperability | No — registry/proxy non-goals |
| `interfaces/IERC5267.sol`, `IERC5313.sol`, `IERC6372.sol`, `IERC5805.sol` | PARTIAL | `Context`, `Permit`, `Sdk.Eip712Domain`, `Sdk.Ierc5313`, `Sdk.Ierc6372`, `DomainLink`, `OwnerLink`, `ClockLink` | EIP-712 domain, owner, clock/votes views | Static domain fields: W4 slice 2; owner/clock views: W5 slice 5b; votes (IERC5805) blocked by votes model |
| `interfaces/draft-IERC3009.sol`, `IERC7674.sol`, `IERC7751.sol`, `IERC7786.sol`, `IERC7802.sol`, `IERC7821.sol` | PARTIAL | `Sdk.Erc3009`, `Auth3009Link` | Bounded `transferWithAuthorization` and `receiveWithAuthorization`; no cancellation or cross-chain/account protocols | Yes — this expansion shipped `receiveWithAuthorization` (distinct EIP-712 typehash, `caller == to`); cancellation and remaining draft interfaces remain blocked |
| `metatx/ERC2771*.sol` | ABSENT | — | Trusted-forwarder sender/calldata suffix | No — raw/rewritten calldata is prohibited |
| `proxy/**`, `interfaces/IERC1967.sol` | ABSENT | — | Delegate proxy, beacon, clones, UUPS | No — `delegatecall`, proxy, CREATE2 are permanent non-goals |
| `token/ERC20/**` | PARTIAL | `Sdk.Fungible`, `Sdk.Erc20Meta`, `Sdk.SafeErc20`, `Erc20Meta`, `Token`, `SafePay` | No complete extensions/permit-votes ecosystem; `Token` keeps packed `bytes32` metadata; mint gap closed on `Erc20Meta` (owner-gated); `Erc20Meta` ships issuer `permit` / `DOMAIN_SEPARATOR` / `nonces` | Yes — W5 slice 4 shipped `Sdk.Erc20Meta` + `Fungible.Log` typed Transfer/Approval on `Erc20Meta`; W5 slice 6 closed owner-gated mint on `Erc20Meta`; this expansion shipped issuer permit on `Erc20Meta`; extensions/permit-votes remain blocked |
| `token/ERC721/**` | PARTIAL | `Sdk.Erc721`, `Sdk.Erc721.Enum`, `Sdk.MetadataUri`, `Collectible`, `Badge`, `Gallery`, `ArtLink`, `ReceiverLink` | Core, both `safeTransferFrom` overloads with the outbound receiver check on `Collectible`, bounded receiving side on `ReceiverLink`, bounded metadata, bounded IERC721Enumerable on `Gallery` | See IERC721 row |
| `token/ERC1155/**` | PARTIAL | `Sdk.Erc1155`, `Sdk.MetadataUri`, `MultiToken`, `CraftToken`, `PackLink`, `ReceiverLink` | Single-id core, `safeTransferFrom` with the outbound receiver check on `CraftToken` and `MultiToken`, bounded `balanceOfBatch` and `safeBatchTransferFrom` (four slots, `DuplicateId()` on duplicate ids, outbound `onERC1155BatchReceived`) on `MultiToken`, bounded receiving side on `ReceiverLink`, bounded metadata | See IERC1155 row |
| `token/ERC6909/**` | PARTIAL | `Sdk.Erc6909`, `NineLink` | Fixed single-id profile only; no dynamic multi-id registration | Yes — W5 slice 8 shipped bounded fixed-id ERC-6909 using existing map primitives |
| `token/common/ERC2981.sol`, `ERC1363Utils.sol` | PARTIAL | `Sdk.Erc2981`, `RoyaltyArt` | Static royalty only; no transfer-and-call | Royalty: W2 shipped; transfer-and-call: no callbacks |
| `utils/Context.sol`, `Pausable.sol`, `ReentrancyGuard*.sol` | PARTIAL | `Sdk.Base.Context`, `Sdk.Pausable`, `Sdk.Reentrancy` | Explicit policy helpers, not Solidity inheritance/transient storage clone | Pause events shipped S4b; no further W3 event gap |
| `utils/Address.sol`, `LowLevelCall.sol`, `Multicall.sol`, `RelayedCall.sol`, `SimulateCall.sol`, `Calldata.sol`, `Memory.sol` | ABSENT | `OpenCall` is typed and closed | Arbitrary call/data/delegate/value helpers | No — arbitrary calldata/call and delegatecall prohibited |
| `utils/cryptography/**` | PARTIAL | Keccak/SHA-256, fixed EIP-712 permit path, `Sdk.Ecdsa`, `Sdk.Ierc1271.checkNow`, precompile internals | No general Merkle/SignatureChecker public SDK beyond the 65-byte `checkNow` profile and bounded recover | Bounded Merkle proof: W4; public typed ecrecover: W5 slice 5b (`Sdk.Ecdsa`, `RecoverLink`); signature checker: `Sdk.Ierc1271.checkNow` is OZ `isValidSignatureNow` (65-byte signature, revert instead of `false`); `checkSignature` is the 1271-only half; `Ecdsa.recover` stays the `(v, r, s)` EOA helper |
| `utils/math/**`, `SafeCast.sol`, `Panic.sol`, `Errors.sol`, `Comparators.sol` | PARTIAL | `Core.Math`, `Core.SafeCast`, `UInt256`, closed `Revert` | Not full Solidity numeric/type/error families | Existing IR already covers the bounded UInt64 math / SafeCast profiles; remaining families stay PARTIAL |
| `utils/structs/**`, `Arrays.sol`, `Bytes.sol`, `Strings.sol`, `Base*`, `RLP.sol`, `Packing.sol`, `ShortStrings.sol` | PARTIAL | Bounded vec/ring/bitmap/enumerable structures, fixed bytes | No general dynamic bytes/strings/arrays/enumerable map semantics | Fixed-capacity variants only — W4; general versions no |
| `utils/Create2.sol`, `Create3.sol`, `StorageSlot.sol`, `SlotDerivation.sol`, `TransientSlot.sol`, `draft-InteroperableAddress.sol` | ABSENT | — | Contract creation, arbitrary slots, transient/interoperable address encodings | No — creation/slot escape hatches non-goals |
| `utils/Blockhash.sol`, `BlockHeader.sol`, `ERC6372Utils.sol`, `Nonces*.sol`, `RateLimiter.sol` | PARTIAL | `Sdk.Nonces`, `Sdk.RateLimit`, `Sdk.BlockHeader`, `Sdk.Ierc6372`, `EvmQuota`, `HeaderLink`, `ClockLink`, `Context.blockHash`, block fields, hashed/static storage | Fixed-window counter only; no token-bucket refill or sliding window | Nonce/rate profile: W3 slice 2; header profile: W5 slice d (`Sdk.BlockHeader`, `HeaderLink`); bounded ERC6372 clock helpers: W5 slice 5b (`Sdk.Ierc6372`, `ClockLink`) |
| `vendor/**`, `mocks/**` | ABSENT | — | Upstream dependencies and test fixtures | No — not runtime SDK surface |

## Prioritized implementation waves

| Wave | Scope | Status |
|---|---|---|
| W1 | Static ERC-165 core (`bytes4` IDs, bounded support predicates), adopt `supportsInterface` in both ERC-721- and ERC-1155-shaped examples, compiler/ABI and Anvil gates | Verified on `cursor/oz-sdk-w1-e4eb`: targeted and full `lake build Tests`, plus four Anvil gates. The examples advertise `IERC165` only: their partial ERC-721/1155 method surfaces must return false for the standard token IDs. |
| W2 | ERC-20-shaped consumer ergonomics: explicit safe-transfer/allowance policy helpers and a static ERC-2981 royalty profile | Verified on `cursor/oz-sdk-w2-e4eb`: targeted `Tests.EvmSafeErc20Spec` / `Tests.EvmErc2981Spec` and full `lake build Tests` (218 jobs), plus `anvil_safepay.sh` and `anvil_royaltyart.sh`. `forceApprove` is always `approve(0)` then `approve(amount)`. With a nonzero receiver, `RoyaltyArt` advertises IERC165 + complete IERC2981, returns false for IERC721/IERC1155, and quotes the full UInt256 sale-price range; a zero receiver advertises IERC165 only. |
| W3 | Bounded access/utility closeout: Ownable2Step start event and constructor-init policy where extractable; fixed-capacity role profiles; bounded nonce/rate helpers | Verified on `cursor/oz-sdk-w3-e4eb`: slice 1 (PR #18, Ownable2Step LOG3 `OwnershipTransferStarted`, `canInit`); slice 2 (`Sdk.Nonces`, `Sdk.RateLimit`, `EvmQuota`, `Roles.Set4`/`EvmCrew` with LOG4 role events, closed nonce/rate failures, `anvil_evmquota.sh` + `anvil_evmcrew.sh`, full `lake build Tests` (228 jobs)) |
| W4 | Static metadata/finance/crypto profiles: bounded ERC-721/1155 URI response, EIP-5267-style static domain fields, single-beneficiary vesting, bounded Merkle proofs | Verified on `cursor/oz-sdk-w4-e4eb`: slice 1b bounded ERC-721/1155 URI (`MetadataUri`, `ArtLink`, `PackLink`); slice 2 EIP-5267-style split domain fields (`Eip712Domain`, `DomainLink`); slice 3 bounded native-ETH vesting with constrained partial release and ordered reentrancy lock (`Vesting`, `VestLink`, `anvil_vestlink.sh`); slice 4 true depth-8 bounded Merkle proof folding (`MerkleProof`, `ProofLink`, sorted-pair ABI-order `keccak256`, fail-closed zero-root/over-capacity gates, `Tests.EvmMerkleProofSpec`, `anvil_prooflink.sh`). |
| W5 | Completion audit: re-inventory the then-current OZ tree, prove every remaining item is either DONE, a restricted PARTIAL profile, or blocked by a documented non-goal | Slice 1 on `cursor/oz-sdk-w5-e4eb`: authority tree re-inventory unchanged (`641ba990`, 452 paths / 367 `.sol`); `Sdk.OzAudit` compile-time counters (32 rows: 2 DONE / 16 PARTIAL / 14 ABSENT), `AuditLink` witness, `Tests.EvmOzAuditSpec`, `anvil_auditlink.sh`. Slice 2: bounded static table with per-row path tag + DONE/PARTIAL/ABSENT + blocker bit, `statusOf`/`isBlocked`/`pathTagOf`/`allRowsClassified` predicates, extended `AuditLink` + spec + Anvil loop over all 32 rows. Slice 3: permanent non-goal category tags (`nonGoalTagOf`, six documented categories + `nonGoalNone`, `allBlockedRowsTagged`, `auditOkEvidence`) linking each of the 14 ABSENT rows to `§ Permanent non-goals` evidence; extended `AuditLink` + spec + Anvil non-goal checks. Slice 4: ERC-20 metadata/event closeout — `Sdk.Erc20Meta` bounded string name/symbol publish gates, `Fungible.Log` typed canonical Transfer/Approval, `Erc20Meta` consumer refactor, `Tests.Erc20MetaSpec` typed-event ABI pins, `anvil_erc20meta.sh`. Slice 5: strict-audit closeout of bounded runtime `renounceOwnership` on `TwoStepCounter`/`Credits`, clearing owner and pending nominee and emitting canonical `OwnershipTransferred(owner,address(0))`; constructor effects remain blocked. Slice 6: owner-gated mint on `Erc20Meta` (fail-closed `Unauthorized`/`ZeroAddress`, canonical mint `Transfer` from zero, `totalSupply` tracks minted supply); `anvil_erc20meta.sh` mints via transaction (no storage injection); rows 9/20 mint gap closed — IERC20/`token/ERC20` rows stay PARTIAL (extensions/permit-votes ecosystem blocked). Slice d: `Sdk.OzAudit` blocker model honesty (`isBlocked` independent of `isAbsent`, `isTemporaryGap`/`blockedImpliesAbsent`, `temporaryGapCount`); bounded Blockhash/BlockHeader profile (`Sdk.BlockHeader`, `HeaderLink`, `Tests.EvmBlockHeaderSpec`, `anvil_headerlink.sh`); row 30 header gap closed. Slice 7: bounded ERC-4626 vault profile — `Sdk.Erc4626`, `Vault4626Link`, `Tests.EvmErc4626Spec`, `anvil_vault4626link.sh`; row 13 PARTIAL. Slice 8: bounded fixed-id ERC-6909 — `Sdk.Erc6909`, `NineLink`, `Tests.EvmErc6909Spec`, `anvil_ninelink.sh`; row 22 PARTIAL. Slice e: bounded delayed default-admin profile (`Sdk.DefaultAdminDelay`, `AdminDelayLink`, row 2 PARTIAL) and typed bounded ERC-3009 transfer-with-authorization (`Sdk.Erc3009`, `Auth3009Link`, row 16 PARTIAL); `Tests.EvmDefaultAdminDelaySpec` / `Tests.EvmErc3009Spec`, `anvil_admindelaylink.sh` / `anvil_auth3009link.sh`. Branch bases on W4 + merges `main` for W3 slice 2 deps. |

W5 slice 5b is also stacked on the completion branch: `Sdk.Ierc5313`/`OwnerLink`,
`Sdk.Ierc6372`/`ClockLink`, and public typed `Sdk.Ecdsa.recover`/`RecoverLink`, with focused Lean
and Anvil gates.

## Expansion phases after W5

W5 closed the audit with every row DONE, a named PARTIAL profile, or blocked by a documented
non-goal, and `temporaryGapCount` 0. Phase 3 reopened one row honestly: `interfaces/IERC1271.sol`
became ABSENT but no longer blocked, because `OpenCall.callMagic` is the call/result protocol its
blocker named, so `temporaryGapCount` was 1 and `blockedCount` 9 until `Sdk.Ierc1271` landed.
It has: the row is PARTIAL (`partialCount` 21, `absentCount` 9, `blockedCount` 9,
`temporaryGapCount` 0). `AuditLink` keeps `isTemporaryGap` and `temporaryGapCount` beside
`isBlocked`, so the next reopened row has a home, and its gate checks that every ABSENT row is
exactly one of blocked-and-tagged or gap-and-untagged. The phases below widen the PARTIAL
profiles toward the
contract shapes people build most often. Each phase lands as its own PR with a Lean spec, an
Anvil gate listed in `runtime-tests/evm/anvil.sh`, and a row edit in the coverage table. The
`Sdk.OzAudit` counters must equal the coverage table at every commit.

Reopening a line under § Permanent non-goals takes one PR that edits that line, changes the
matching `Sdk.OzAudit` row or tag with new `Tests.EvmOzAuditSpec` pins, and ships the Anvil gate.
A phase that only widens a PARTIAL profile does not touch that section.

| Phase | Goal | What changes | Evidence | Non-goal touched |
|---|---|---|---|---|
| 0 | Prove and document that one contract calls another without a proxy. Close the gate drift. | `anvil.sh` runs `clocklink`, `ownerlink`, `recoverlink`, and the new `anvil_compose.sh` (`EvmOpenCall` CALLs `Erc20Meta`, both `pf`-compiled). `scripts/check_anvil_suite.py` fails CI when a committed gate is not run. `writing-contracts.md` § Call another contract. Website Limits copy no longer says "dynamic callee". | `anvil_compose.sh` ok; `check_anvil_suite.py` ok (75 gates); counters unchanged | None |
| 1 | Richer typed call shapes for composition: STATICCALL returns typed as `Bool` and `Address`, three and four exact words, and one bounded `bytes` argument. | Shipped: `OpenCall.StaticShape` (`word`, `words2`, `words3`, `words4`, `bool`, `address`) names each read's `CallResult.Policy` and limb count; `Sdk.OpenCall.staticWords3` / `staticWords4` / `staticBool` / `staticAddress`; `Decode` recognizes the stubs through one name table; `CallResult.Emit.wordLimb` projects an address word into `Addr20` byte limbs (shared with `ecrecover20`). Reads stay entry-return only, as `staticWord` already was. Shipped (1b): one bounded `bytes` argument. A `BoundedBytes n` field of the payload constructor is `OpenCall.ArgType.bytes n` (`n ≤ 63` then, the entry-side local-frame ceiling; Phase 3 gave packed bytes their own ceiling of 65); the head carries its offset word, the tail carries the runtime length and only the first `length` bytes over a zeroed region, and `inSize` is the static head plus `ceil32(length)`, so the calldata is byte-identical to `abi.encodeWithSelector`. Works on CALL and STATICCALL. One `bytes` argument per plan (`maxBytesArgs`), at any position; `BoundedString` arguments and a source-built `{ data with length := k }` argument stay fail closed (the second is a decoder base-resolution gap, not a plan rule). The Phase 3 signature spelled as `BoundedBytes 65` could not enter then: `packedBytesV1InputPlan` admitted `1 + capacity ≤ 64`; Phase 3 raised the packed ceiling without moving the array one. | `Tests.EvmOpenCallSpec` pins policy and limb count per shape, the bool/address word gates, the 96- and 128-byte frame gates, the limb count each entry binds, and for the bytes tail the offset word, guarded `mstore8` per byte, `and(add(len, 31), not(31))`, `add(100, …)` calldata size, the `sink(uint256,bytes)` selector, and the fail-closed second tail, over-ceiling capacity (66 since Phase 3), and string field; `anvil_opencall.sh` proves each fail-closed frame (bool word `2`, dirty address high bytes, wrong-size frames, EOA) and, for `sinkBytes` / `hashBytes`, that the callee decodes the length and payload and that the whole calldata of both the CALL and the STATICCALL hashes equal to `cast calldata` for lengths 0, 3, and 8; `anvil_compose.sh` reads `balanceOf`, `ownerOf`, and a `pf`-compiled `Badge`'s `supportsInterface` (`true` and `false`) between `pf`-compiled contracts | None. Arbitrary calldata stays out; the one typed bounded argument is in |
| 2 | Bounded ERC-1155 batch: `safeBatchTransferFrom` and `balanceOfBatch` over `BoundedVec UInt256 n`, and a bounded `TransferBatch` event. | Shipped first: `Erc1155.batchCapacity` (4), `Erc1155.Batch`, the `batchLength` rule (equal lengths answer `owners.length`, unequal answer `0`), `Erc1155.Balances.balanceOfBatch` (one checked read per slot), and `MultiToken.balanceOfBatch`. The view needed three compiler fixes the Phase 0 spike could not see because it echoed its argument: a constructed `BoundedVec` result publishes every limb of a `UInt256` or `Address` element and distributes a wide `if` over its limbs (`Extract.Decode`); a `.select` branch no longer leaks values it materialized into the enclosing scope (`Evm.Emit`); a dynamic result computes every element into a local before it stores the frame, because element code hashes map keys in the same low memory (`Evm.Codec.Emit`). Shipped next: `MultiToken.batchTransferFrom(address,address,uint256[],uint256[])` over the same four slots, in OZ check order. Operator authorization with the key envelope over every id, nonzero receiver, equal lengths, pairwise distinct ids, then one checked movement per slot. Unequal lengths revert `ERC1155InvalidArrayLength(idsLength, valuesLength)` (selector `0x5b059991`). The first short slot reverts `ERC1155InsufficientBalance(sender, balance, needed, tokenId)` (`0x03dee4c5`). A wrapping credit stops in the checked `add256` with an empty revert, as `mint` does. Duplicate ids revert `DuplicateId()`, this ledger's own bound: with distinct ids no two slots touch one `(owner, id)` key, so checking every slot before the first write equals OZ's sequential checks. OZ accepts duplicates by applying slots in order; here the caller splits the batch. Success persists the active slots in array order and logs by OZ's `_update` rule (`Erc1155.Log.transferBatchOrSingle`): a one-slot batch logs `TransferSingle`, any other length logs one `TransferBatch` whose `ids` and `values` are exactly the submitted slots. The batch later took the `safe` prefix once Phase 3 shipped `onERC1155BatchReceived` through two array OpenCall args. The single transfer is `safeTransferFrom`. It needed four compiler commits. Typed error fields widened from `UInt64` to the typed-event scalar vocabulary (narrow and wide integers, `address`, fixed bytes, bool), so OZ error selectors survive. `Extract.Decode` keeps a map read inside an error field on the typed frame instead of turning the revert into a `uint64` return. An EVM effect under `if` or `dite` extracts as a structured `.ite` op (it was flattened to unconditional, a fail-open), and a `let`-bound effect runs once (it ran twice). The emitter got under EIP-170 by root cause rather than by shrinking the batch. A select over leaf branches is one guarded assignment (a comparison that selects 1/0 is the comparison), the address limb shuffle is three runtime helpers `pf_addr_w0..2` instead of twenty-two inlined opcodes per use, and limbs split from one ABI word pack back into that word (`Codec.Emit.addrWordOfLimbs`, `u256WordOfLimbs`). Every registered program shrank: 373,297 to 259,739 runtime bytes over 71 programs, `MultiToken` 34,681 to 23,726 with the batch, `Token` 23,374 to 18,586. Shipped last: the bounded `TransferBatch` event, which reopened the "dynamic arrays in events" non-goal as a bounded shape in three compiler commits. `LogError.LogPlan` carries at most two `LogTailPlan`s after its static words; the head grows by one offset word per tail and `emitLog` writes each tail's offset, its runtime length (reverting if it exceeds the capacity), and only the first `length` slots, with the LOG data length as a block-scoped cursor. `NativeFx.Call.logTyped` carries `Array LogTail` beside the scalar `EventFrame` (Core's frame is unchanged, Common stays at `main`); topic0 and ABI JSON both derive from `logTypedAbiTypes`, which spells a tail as `T[]`, so `TransferBatch(address,address,address,uint256[],uint256[])` hashes to the ecosystem topic0 `0x4a39dc06...`. The extractor turns a non-indexed `BoundedVec` event field over a closed scalar into a tail (its length leaf plus every slot's limbs) and fails closed on an indexed array, a scalar declared after an array, a non-scalar element, or a third tail. Two bounds are deliberate: tails are the last declared fields (one decoder check instead of a position threaded through signature, JSON, and head order), and a tail holds at most 63 elements (`Codec.maxBoundedArrayLocalWords`). `Erc1155.Notice.TransferBatch` spells `BoundedVec UInt256 4` because the decoder, like the entry profile, wants a literal capacity. Duplicate ids fail closed with `DuplicateId()`; they are not an open gap. Open: interleaved dynamic fields (Governor `ProposalCreated`, a permanent non-goal). The result of a bounded view must still be built as `{ length, values := #v[...] }`; a `{ v with length := ... }` update is refused. | `Tests.EvmErc1155Spec` pins the length rule and the empty answer on the host, the 17-leaf frame, the `(address[],uint256[]) -> uint256[]` ABI, the compute-then-publish Yul order, and for `batchTransferFrom` the pure batch predicates on the host, five typed error frames in OZ field order, one selector-only `DuplicateId`, the OZ `TransferSingle`-or-`TransferBatch` frame pair with the two `uint256[]` tails read from the entry's own arrays (length leaf plus sixteen limbs each), the `TransferBatch` ABI JSON and topic0, the nine-method ABI, the `cast sig` selectors, and the `revert(0, 132)` geometry. `Tests.EvmTypedErrorSpec` pins the wide error frames, the OZ selectors, the map read inside an error field, and that an argument word is never repacked from its own limbs. `Tests.EvmEffectStateSpec` pins the `.ite` op and the once-per-let effect. `Tests.CoreCodecSpec` pins the word identities and the select peephole shapes byte-exact. `anvil_multitoken.sh` deploys the 18,925-byte program (EIP-170 is the size gate; the pair-map load helper paid for the batch hook) and drives `safeBatchTransferFrom`: unauthorized operator and alias id (`Unauthorized`), zero receiver, both unequal-length shapes with the OZ words decoded, duplicate id in slot 2, a short second slot that reverts with `(sender, 30, 31, id)` and leaves slot 0 unwritten, a wrapping credit as the empty checked-add revert, a two-slot success whose receipt decodes by ABI layout to `TransferBatch(ids=[7, 9], values=[10, 30])` (canonical offsets, exact data length, no `TransferSingle`) and `balanceOfBatch` `[40, 55, 0, 30]`, an empty batch that logs `TransferBatch([], [])`, a one-slot zero-amount batch that takes the `TransferSingle` branch, a four-slot operator batch decoded as one `TransferBatch` then revoked, and a fifth slot rejected by the decoder. `Tests.EvmLogErrorSpec` and `Tests.EvmTypedEventSpec` pin the tail plan bounds, the byte-exact LOG4 text with two length-guarded tails, the `uint256[]` ABI JSON, tail-name conflicts, and the four decoder rejections. Eight mutations went red across the two slices. The committed DuplicateId Yul mutation in `anvil_multitoken.sh` (selector `0xb1fb6bd0` jumps to the next case) lets a same-id batch succeed; the pristine artifact still reverts `DuplicateId()`. Dropping the (0,3) pair from `distinctIds` fails the host guard. Inverting the emitter's comparison collapse fails `Tests.CoreCodecSpec` and makes the gate revert `Unauthorized` at the owner's first `mint`. On the assembled `TransferBatch` artifact, a second offset word off by 32 fails "offset 192 is not the canonical 160", a data length one word long fails "holds 9 words, canonical encoding is 8", slot 1 rewritten to slot 0's id fails on the decoded ids, the OZ branch inverted fails "TransferBatch log count 1, got 0", and a length word incremented fails on a third value. `anvil_ninelink.sh` pins an operator moving 20 past a 15 allowance and leaving the allowance at 15. | The "ERC-1155 dynamic batches" line now reads bounded batches of at most four slots with a bounded `TransferBatch` and outbound `onERC1155BatchReceived`; rows 10 and 21 stay PARTIAL with the remaining gap unbounded arrays (`DuplicateId()` is the batch bound, not a gap), `temporaryGapCount` stays 0 |
| 3 | Outbound receiver and signer checks through typed CALL with the `magicBytes4` policy: `onERC721Received`, `onERC1155Received`, and ERC-1271 `isValidSignature` with a `BoundedBytes 65` signature. | The bounded `bytes` argument landed in Phase 1b. Shipped (3a): a STATICCALL read is a value. `Extract.Decode.openStaticRead?` lowers `OpenCall.staticWord` / `staticBool` / `staticAddress` (and the first word of `staticWords2..4`) to the `openCall` component query at the requested limb, and `asValNamed` / `asBoolVal` accept it wherever a `UInt256`, `Address`, or `Bool` stands: an `if` condition, a comparison operand, another call's argument. A read used in several limbs runs once (the emitter caches the wide word per plan), and `selectOrArm` collapses a select with equal arms, so a CALL guarded by a read does not run the read twice. Two compiler fixes came out of it. `OpenCall.Emit.emitPlan` materializes every operand before the first calldata store, because a read used as an argument runs its own STATICCALL through `memory[0, …)` and overwrote the outer selector. `Extract.Decode.collectEvmQueryOps` descended into operator arguments and `let` values and claimed any read as the body's result. `covers` returned the balance word instead of `ge`, and on `main` `VestLink.releasable()` and `vestedAmount()` return `selfbalance()` instead of the linear formula (the gate only checked before the start and after the end, where the two agree). The scan now follows only `let` bodies, lambdas, and inline facades, and the effectful-`let` checks use the deep `containsEvmEffect`. Eight program digests moved. The Yul diff is the VestLink formula, a canonical `iszero(eq(w, 0))` on Bool views over a map read, and UInt256 return words packed through the value decoder. Shipped (3b, carrier refusal): a CALL carrier anywhere but the entry's result word is refused at extraction. `Extract.Decode.findOpenCallCarrierMisuse` walks the body once from `decodeBody` with a two-state `CarrierSlot`: `word` along the spine to the result word (through `Except.ok`, the result pair's second component, `Effect.thenTrue`, `if` branches, lambda and `let` bodies), `operand` everywhere else (operator and comparison arguments, an `if` condition, call arguments, state fields, and the value of a `let` whose binder is dropped). A plan in an operand fails with the `CALL carrier out of place` reason. Before it, the computed-with shapes lowered to the CALL followed by the constant `0` (`callSuccess t ping == 0` answered `false` on Anvil where Lean answers `true`, `+ 1` answered `0`, a compared `if` never ran the CALL, a state field stored `0`, an inner CALL or STATICCALL argument was dropped), a dropped `let` above an `if` lost the `if` and its stores, and `if Effect.thenTrue (call) then …` stored without running the CALL. No registered program moved (71 digests unchanged). Shipped (3b, magic): `OpenCall.callMagic` is a CALL plan with `CallResult.Policy.magicBytes4` set to the plan's own selector, computed in `Extract.Decode.decodeOpenCallCtor` from the constructor (`Plan.selectorHex`); the emitter path is the existing `CallResult.Emit` magic gate (exact 32-byte frame, zero low 28 bytes, equality to the left-aligned selector). `Examples.Evm.EvmOpenCall.notifyReceiver` calls `onERC721Received(address,address,uint256,bytes)` on a receiver. Shipped (3b, hooks): `Sdk.Erc721.Hook` / `Sdk.Erc1155.Hook` spell `onERC721Received(address,address,uint256,bytes)` and `onERC1155Received(address,address,uint256,uint256,bytes)` with a literal `BoundedBytes 32` data field (the open-call decoder wants a monomorphic enum, so the capacity is not a parameter), and `checkOnReceived` is OZ's `checkOnERC721Received` / `checkOnERC1155Received`: `Address.hasCode` decides whether the hook runs, `OpenCall.callMagic` demands the selector back. The result is a CALL carrier, so a consumer writes `Effect.thenTrue (Erc721.checkOnReceived to Context.caller source tokenId data)` as the result word with the ledger movement and the log in the state word; the extractor orders stores, then log, then hook, so a receiver reading `ownerOf` or `balanceOf` inside the hook sees the post-transfer state. `Collectible.safeTransferFrom(address,address,uint256,bytes)` (4,905 to 10,227 runtime bytes, then 8,469 after packed-bytes `calldatacopy`) and `CraftToken.safeTransferFrom(address,address,uint256,uint256,bytes)` in place of its `transferFrom` (7,498 to 11,123, then 9,092) use it. `MultiToken.safeTransferFrom` now uses it too. It was 22,386 without the hook and 23,974 with it in place of `transferFrom` (602 under EIP-170) after packed-bytes `calldatacopy`. Adding the hook beside `transferFrom` still overflowed. The batch hook later needed the pair-map load helper; current MultiToken is 18,925. The decoder keeps packed `bytes` in calldata (`PackedBytesFrame`) and `OpenCall.Emit` copies the padded tail with one `calldatacopy`; SignerLink dropped from 4,708 to 1,140. Shipped (3b, IERC1271): packed `bytes` and `string` frames have their own ceiling, `Codec.maxPackedBytesCapacity` 65 (one ECDSA `r ‖ s ‖ v`), read by the entry plan, both packed result-frame checks, and `OpenCall.ArgType.supported`; bounded arrays, event tails, and `Emit.memoryGuardBytes` keep `maxBoundedArrayLocalWords` 64, because a 65-byte packed frame is 66 Yul locals but 32 + 96 bytes of memory, so no shipped program moved (71 digests and `memoryguard(4096)` unchanged against the pre-change build). `Sdk.Ierc1271.Check` spells `isValidSignature(bytes32 hash, bytes signature)` with a literal `BoundedBytes 65`, and `checkSignature signer hash signature` is `OpenCall.callMagic` on it: OZ `SignatureChecker.isValidERC1271SignatureNow` as a fail-closed CALL carrier, with no `hasCode` branch because a signer without code answers an empty frame the magic policy already refuses. `SignerLink.requireSigner(address,bytes32,bytes)` (1,140 runtime bytes after packed-bytes `calldatacopy`) counts accepted checks in the state word and carries the check under `Effect.thenTrue`. Shipped (3c, array args): `OpenCall.ArgType.array` (`maxArrayArgs` 2, `maxDynamicArgs` 3). A plan with an array walks a byte cursor so later tails follow a runtime-length prefix. Plans with no array keep the compile-time offset path, so `sinkPlan.inSize` stays 100. `Sdk.Erc1155.Hook.onERC1155BatchReceived` and `checkOnBatchReceived` take two `BoundedVec UInt256 4` plus `BoundedBytes 32`. `MultiToken.safeBatchTransferFrom` and `EvmOpenCall.notifyBatchReceiver` consume it (selector `0xbc197c81`). The first `pf build MultiToken` after the hook was 25,364 bytes (788 over EIP-170). Packed-array `calldatacopy` made it worse (26,232). Pair-map load helpers `pf_load_pair256` / `pf_load_pair_u64` (one copy of the seven-word key hash plus tagged sload that solc would not share across CFG cases) brought MultiToken to 18,925 (5,651 under). Pair-map sets stay inline. Shipped (3b, isValidSignatureNow): `Ierc1271.checkNow` is the combined OZ gate. A signer with code takes `checkSignature`. A signer without code splits the 65-byte `r ‖ s ‖ v` through `packLane` / `Ecdsa.recover` (`SignerLink.requireNow`, digest `48768ba3013eb87`, 9,115 runtime bytes). Length is an `if`, not `&&`, because `Bool.and` is eager. Packed-bytes `calldatacopy` shipped. | Shipped (3a): `Tests.EvmOpenCallSpec` pins the canonical IR of each read shape (the Bool read as the `ite` condition with an empty else, the UInt256 read under `ge256`, the Address read under `eq20`, the inner read as the outer read's argument), the operand-before-store order through a context whose operand prelude reads scratch memory (for an argument and for the CALL value), and that a compared CALL word lowers as the CALL plus the dummy word with no `Query`. `Tests.EvmVestingSpec` pins `checkedDivMod256` around `selfBalance256` in `releasable` and `vestedAmount`. `anvil_opencall.sh` drives `pingIfOn`, `echoIfOn`, `covers`, `ownedBy`, and `echoBalance` to both outcomes against the Solidity mock, proves the untaken branch never calls (echo(0)'s two-word frame would revert), and keeps the strict-bool and canonical-address gates fail-closed inside a guard and a comparison. `anvil_vestlink.sh` checks a quarter, a half, and the end of the schedule; the pre-fix VestLink artifact fails the quarter with the raw balance. Mutations red: restoring the deep descent fails the `covers` pin and the VestLink pin; interleaving an argument prelude with the stores fails the order guard; on the assembled artifact an inverted guard, the false arm taking the read, the comparison replaced by the read word, address equality forced true, and one forwarded limb each fail the gate. Carrier refusal: `Tests.EvmOpenCallMisuse` holds twelve refused entries (compared, `== 0`, `+ 1`, a state field, a compared `if`, a STATICCALL argument, another CALL's argument, a compared `callValue`, `if Effect.thenTrue (call)`, `Effect.thenTrue (call) == false`, a dropped `let`, a dropped `let` above an `if`) and `Tests.EvmOpenCallSpec` pins each through `extractMethod` plus three kept homes (the result word under `thenTrue`, `Effect.ensure`, a `let` whose binder reaches the word); `anvil_opencall.sh` runs `pf build --module Tests.EvmOpenCallMisuse` and fails unless it is refused for the carrier reason with no artifact written. Mutation red: with the `decodeBody` check disabled the spec stops at its first carrier pin (`compared` extracts), `pf build` compiles the module and the gate fails, and on Anvil that artifact answers `isZero` false, `plusOne` 0, runs no CALL in `gated`, drops the inner CALL of `callArg` and the STATICCALL of `readArg`, skips the guard and the store in `letGuarded`, and stores in `thenTrueGated` without a CALL. Magic hook: `Tests.EvmOpenCallSpec` pins the `notifyReceiver` plan (`kind .call`, `policy .magicBytes4 "150b7a02"`, four args with the `bytes 8` tail, one `bytes4` word) and the Yul equality gate, and refuses `callMagic` compared like every other carrier; `anvil_opencall.sh` drives `notifyReceiver` against the Solidity mock's settable hook frame: the right magic (operator, from, token id, and payload keccak observed by the callee), then a wrong selector, a dirty low byte, an empty frame, a four-byte frame, a two-word frame, and an EOA, each reverting. Mutations red on the assembled artifact: the magic equality dropped (a wrong selector passes), the size check dropped (a two-word frame passes), and all three gates dropped. Hooks: `Tests.EvmErc721Spec` and `Tests.EvmErc1155Spec` pin each `safeTransferFrom` (the `Transfer` / `TransferSingle` frame beside one CALL plan under `magicBytes4` of its own selector, `150b7a02` with four head words and the 32-byte tail at 164 static calldata bytes, `f23a6e61` with five at 196, the ABI entry, the Yul `extcodesize` guard and magic gate), run `checkOnReceived` on the code-less host stub, and repin the two digests; the pins were red before the SDK change (`checkOnReceived` unknown, `safeTransferFrom` missing, CraftToken's method list still naming `transferFrom`). `runtime-tests/evm/ReceiverMock.sol` answers both hooks with a settable frame or a revert carrying it, records the arguments, and reads `ownerOf` / `balanceOf` back through `msg.sender` inside the hook; `anvil_collectible.sh` and `anvil_crafttoken.sh` drive the right magic (the receiver saw operator, from, id, value, the payload keccak, and its own post-transfer ownership or credited balance, 5 then the running 7 then 8 after an operator move), an approved operator as `operator` with the holder as `from`, an EOA recipient not called, and five refusals each reverting empty with the owner and both balances left in place (a wrong selector, a dirty low byte, an empty frame, a two-word frame, a receiver reverting with the magic word), the restored frame accepted again, and the 32-byte data bound (33 bytes refused, 32 reach the receiver whole). Mutations on the assembled Yul of each `safeTransferFrom` case, each red on the case that observes it and pristine green after: `extcodesize` forced to 0 (the receiver records nothing), forced to 1 (the EOA transfer reverts), the CALL success check dropped (the reverting receiver passes), the frame-size check dropped (two words pass), the magic equality dropped (a wrong selector passes). IERC1271: `Tests.EvmBoundedSpec` and `Tests.EvmOpenCallSpec` pin the ceiling (65 enters an entry, a result, and a plan; 66 is refused with the 66-word message; the array ceiling stays 64), red first. `Tests.EvmIerc1271Spec` pins the `requireSigner` plan (`kind .call`, `policy .magicBytes4 "1626ba7e"`, a `bytes32` head word and the `bytes 65` tail at 100 static calldata bytes), the ABI, the Yul magic gate with no `extcodesize`, and the digest `7edf01a87d0a1652`; red first as a bad import. `runtime-tests/evm/Erc1271WalletMock.sol` is OZ's `ERC1271WalletMock` with a recorder and a settable frame (ecrecover against its owner, records the hash, `keccak(signature)`, and the length); `anvil_signerlink.sh` drives the owner's signature (accepted, the wallet saw the hash, all 65 bytes, and length 65), five signature refusals (another key, a tampered byte, 64 bytes, a different hash, an empty signature), the 66-byte bound refused before the wallet is called, two signers without code (an EOA and a fresh address), a fixed magic frame passing a garbage payload, and five frame refusals (wrong selector, dirty low byte, empty frame, two-word frame, revert carrying the magic), every refusal an empty revert with `accepted` left in place. Mutation red on the assembled Yul: the magic equality dropped, the wallet's `bytes4(0)` word passes and the gate fails at the foreign key. checkNow: `Tests.EvmIerc1271Spec` pins `requireNow` (same 1271 plan plus `extcodesize` and `staticcall(gas(), 1, 0, 128, 0, 32)`), digest `48768ba3013eb87`; `anvil_signerlink.sh` accepts an EOA, refuses a foreign key / 64-byte / empty EOA frame as `Unauthorized(signer)`, accepts the wallet through 1271, empty-reverts a foreign wallet answer, and with `extcodesize` rewritten to `0` still accepts the EOA and treats the wallet as an EOA | 3a: none. `Sdk.OzAudit` rows and counters are unchanged; a compiler capability and two fixes widen no profile. 3b hooks: rows 9, 10, 20, and 21 stay PARTIAL with the receiver gap narrowed to the receiving side and the batch hook (`doneCount` 2, `partialCount` 20, `absentCount` 10 unchanged). Row 12 (`interfaces/IERC1271.sol`) is reclassified from blocked to a temporary gap: `isBlocked 12` false, `nonGoalTagOf 12` none, `isTemporaryGap 12` true, `blockedCount` 9, `temporaryGapCount` 1. `AuditLink` gains `isTemporaryGap` and `temporaryGapCount` (digest `d5cb688dcc4d25ae` to `62c5b84cc38d9613`), `Tests.EvmOzAuditSpec` pins the row and the counts, and `anvil_auditlink.sh` checks every ABSENT row on chain is exactly one of blocked-and-tagged or gap-and-untagged and counts one gap. The note that stood here, that row 12 would flip to PARTIAL when the safe-transfer decisions landed, was wrong: row 12 is IERC1271 and nothing ERC-1271 has shipped; the receiver rows were already PARTIAL. The `nonGoalArbitraryCall` docstring now reads as the receiving side of hooks; calling a known hook with a fixed ABI is in. 3b IERC1271: row 12 moves to PARTIAL (`partialCount` 21, `absentCount` 9, `blockedCount` 9, `temporaryGapCount` 0; `AuditLink` digest `62c5b84cc38d9613` to `ad40c48e855ad5ef`, `isTemporaryGap` false for every row and kept as the home for the next reopened row; `anvil_auditlink.sh` checks `statusOf(12)` is PARTIAL and counts zero gaps). § Permanent non-goals is untouched: the receiving side of `isValidSignature` stays out. 3b isValidSignatureNow: rows 12 and 26 stay PARTIAL (`doneCount` 2, `partialCount` 21, `absentCount` 9, `blockedCount` 9, `temporaryGapCount` 0). § Permanent non-goals is untouched. 3c array args: rows 10 and 21 stay PARTIAL with the remaining gap unbounded arrays. `DuplicateId()` is the fail-closed batch bound versus OZ in-order application (`partialCount` 21, `absentCount` 9, `blockedCount` 9, `temporaryGapCount` 0) |
| 4 | Bounded ERC-20 vesting map. | `Vest20Link.release(address)` pays `releasable(token)` through `SafeErc20.transfer`, logs `ERC20Released`, and stores paid amounts in a hashed token map. Native-ETH `release(uint256)` stays on `VestLink`. Row 5 stays PARTIAL. `temporaryGapCount` stays 0. | `Tests.EvmVest20Spec`; `anvil_vest20link.sh` (two tokens, quarter then remainder, CALL rewritten to STATICCALL goes red) | None |
| 5 | Stored beneficiary plus parameterless ETH `release()`. | `VestLink` / `Vest20Link` store the constructor beneficiary and rotate it with one-step `transferOwnership` (`OwnershipTransferred`). `VestLink.release()` pays `releasable()` (OZ ABI). `release(uint256)` still allows a partial payout. Row 5 stayed PARTIAL after this slice. The cliff and the constructor zero-owner revert remained. `temporaryGapCount` stays 0. | `Tests.EvmVestingSpec` / `Tests.EvmVest20Spec`; `anvil_vestlink.sh` (quarter `release()`, rotate, remainder to the new beneficiary, native send value rewritten to 0 leaves the ETH); `anvil_vest20link.sh` remainder after rotation | None |
| 6 | Constructor-stored OZ cliff. | `VestLink` / `Vest20Link` take `cliffDuration` as a constructor-stored field (start and duration stay immutables). `cliff()` is `start + cliffDuration`. Before the cliff, `vestedAmount` is 0. After the cliff the linear formula still uses `timestamp - start` (OZ `VestingWalletCliff` jump). `cliffDuration = 0` is the linear wallet. `cliffDuration > duration` fails closed. Row 5 stays PARTIAL (constructor zero-owner revert). `temporaryGapCount` stays 0. | `Tests.EvmVestingSpec` / `Tests.EvmVest20Spec`; `anvil_vestlink.sh` (zero-cliff path unchanged, half-duration cliff jumps to 0.5 ETH, over-duration cliff hides views); `anvil_vest20link.sh` cliff jump of 500 tokens | None |
| 7 | Constructor CREATE ZeroAddress revert. | Dropped-let `init` on VestLink/Vest20 plus an Emit revert-only constructor prefix (`ctorOpIsRevertGuard`). CREATE of `address(0)` reverts `ZeroAddress()` (`0xd92e233d`). A constructor-only Yul mutation that drops that revert lets CREATE succeed. Constructor logs were still refused in this unit. Row 5 stays PARTIAL. `temporaryGapCount` stays 0. | `Tests.EvmVestingSpec` / `Tests.EvmVest20Spec` pin constructor IR, Yul `0xd92e233d`, digests `65e199169e0e2b14` / `198b50791da92adb`; `anvil_vestlink.sh` / `anvil_vest20link.sh` (CREATE fails, constructor-guard mutation CREATE succeeds) | None |
| 8 | Constructor `OwnershipTransferred(address(0), owner)` log. | Emit constructor prelude (`ctorOpIsAllowedPrelude`) allows the revert-only ZeroAddress guard plus LOG3 `OwnershipTransferred` whose previousOwner limbs are zero. VestLink/Vest20 `init` else-arm is `Ownable.Log.constructorTransferred`. CREATE of a nonzero owner logs that event. A constructor-only Yul mutation that drops the log still deploys and stores the owner. CALL, map writes, value transfers, and other constructor logs stay refused. Row 5 stays PARTIAL (CREATE selector is `ZeroAddress()`, not OZ `OwnableInvalidOwner(address)`). `temporaryGapCount` stays 0. | `Tests.EvmVestingSpec` / `Tests.EvmVest20Spec` pin constructor IR, Yul topic0, digests `2d53b56c1d9429f9` / `3034d29ecd2cc852`; `anvil_vestlink.sh` / `anvil_vest20link.sh` (CREATE receipt LOG3, log-strip mutation has zero such logs) | None |
| 9 | CREATE and `transferOwnership` `OwnableInvalidOwner(address)`. | `NativeFx.Call.revertOwnableInvalidOwner` clones Unauthorized geometry (`revert(0, 36)`). VestLink/Vest20 CREATE and `transferOwnership(address(0))` use `Revert.ownableInvalidOwner`. The extractor parks `returnU64 0` after that constructor revert so the prelude stays allowed. Vest20Link `release(address)` still reverts `ZeroAddress()` on a zero token. Row 5 stays PARTIAL. Only-owner Unauthorized is still the next remainder, plus the split wallets. `temporaryGapCount` stays 0. | `Tests.EvmVestingSpec` / `Tests.EvmVest20Spec` pin constructor IR, Yul selector `0x1e4fbdf7`, digests `b9471739ac722d35` / `194894e2bbdb47e0`; VestLink 9747 B, Vest20Link 14562 B; `anvil_vestlink.sh` / `anvil_vest20link.sh` (CREATE and `transferOwnership(0)` revert `OwnableInvalidOwner(address(0))`; constructor-guard mutation CREATE succeeds) | None |
| 10 | Only-owner `OwnableUnauthorizedAccount(address)`. | `NativeFx.Call.revertOwnableUnauthorizedAccount` clones Unauthorized geometry (`revert(0, 36)`). `Access.ownerViolation` and `Examples.Evm.Ownable.bump` emit that selector with the caller. ERC-721/1155/1271 keep `Unauthorized(address)`. Row 5 stays PARTIAL (split wallets). `temporaryGapCount` stays 0. | `Tests.EvmVestingSpec` / `Tests.EvmVest20Spec` pin ABI `OwnableUnauthorizedAccount` and refuse `Unauthorized`; digests `fac351201b2369ba` / `6f387586e59335d5` at land; VestLink 9747 B, Vest20Link 14562 B; `anvil_vestlink.sh` / `anvil_vest20link.sh` non-owner `transferOwnership` revert `OwnableUnauthorizedAccount(caller)`; `anvil_ownable.sh` / `anvil_twostep_counter.sh` / `anvil_credits.sh` ok | None |
| 11 | Dual-asset `Vest20Link`. | `Vest20Link` takes native ETH through `receive` and pays `releasable()` with `release()`. ERC-20 `release(address)` stays. `releasedOf()` / `releasable()` / `vestedAmount(uint64)` are the native overloads. VestLink stays the ETH-only smaller profile, including `release(uint256)`. Row 5 stays PARTIAL (no Ownable2Step; ABI `releasedOf` rather than OZ `released`). `temporaryGapCount` stays 0. | `Tests.EvmVest20Spec` pins both `release` selectors, `receive`, and `EtherReleased`; digest `ac851caa6b77b626`; `anvil_vest20link.sh` funds ETH and ERC-20 on one wallet and proves independent books | None |
| 12 | TwoStepCounter/Credits CREATE OwnableInvalidOwner plus OwnershipTransferred. | TwoStepCounter and Credits `init` use the Vest dropped-let: zero owner reverts `OwnableInvalidOwner(address)`; the else-arm is `Ownable.Log.constructorTransferred`. Nominate-zero on `transferOwnership` still reverts `ZeroAddress()`. Row 1 stays PARTIAL. `temporaryGapCount` stays 0. | `Tests.EvmOzPolicyEventSpec` pins constructor IR and CREATE log; `anvil_twostep_counter.sh` / `anvil_credits.sh` CREATE receipt LOG3 and zero-owner CREATE | None |
| 13 | ABI `released()` / `released(address)`. | VestLink view `released` and Vest20Link `released__eth` / `released__token` publish OZ `released()` / `released(address)`. Lean keeps the `__` discriminator because Vest20Link already uses `released` for the hashed map. Row 5 stays PARTIAL (no Ownable2Step). `temporaryGapCount` stays 0. | `Tests.EvmVestingSpec` / `Tests.EvmVest20Spec` pin `released` selectors; `anvil_vestlink.sh` / `anvil_vest20link.sh` call `released()` / `released(address)` | None |
| 14 | Vest Ownable2Step nominate plus accept. | VestLink and Vest20Link `transferOwnership` nominates and logs `OwnershipTransferStarted`. `acceptOwnership` rotates the stored beneficiary and logs `OwnershipTransferred`. Nominate-zero reverts `ZeroAddress()`. CREATE of zero still reverts `OwnableInvalidOwner(address)`. There is no `cancelOwnership` or `renounceOwnership`. Row 5 stays PARTIAL as a named restricted profile. VestLink is the ETH-only smaller example. Vest20Link is the dual-asset wallet. `temporaryGapCount` stays 0. | `Tests.EvmVestingSpec` / `Tests.EvmVest20Spec` pin `acceptOwnership`, `pendingOwner`, `OwnershipTransferStarted`, and `ZeroAddress`; `anvil_vestlink.sh` / `anvil_vest20link.sh` nominate, refuse current-owner accept, then `acceptOwnership` before remainder payout; `scripts/check_vest_ownable2step_honesty.py` | None |
| 15 | VestLink ETH-only named profile. | Dual-asset already lives on Vest20Link. VestLink stays the smaller ETH-only example (`release(uint256)` / `release()` / `released()`). Docs no longer list ETH-only as an implementable remainder. Row 5 stays PARTIAL (Ownable2Step vs OZ one-step Ownable; no post-CREATE schedule mutation). `temporaryGapCount` stays 0. | `scripts/check_vest_eth_only_honesty.py`; `Tests.EvmVestingSpec` refuses `release(address)` / `released(address)` / `ERC20Released`; `anvil_vestlink.sh` calls `released()` only | None |
| 16 | Erc20Meta issuer EIP-2612 permit. | `Erc20Meta.permit` / `DOMAIN_SEPARATOR` / `nonces` clone Token's closed Token/1 path without pause. IERC2612 view is `nonces(address)`, not Token's `nonceOf`. Domain name/version stay the closed Token/1 profile. Rows 8 and 19 stay PARTIAL. `temporaryGapCount` stays 0. | `Tests.Erc20MetaSpec`; `anvil_erc20meta.sh` (typed Permit sign, wrong signer, expired deadline, `transferFrom`); `scripts/check_erc20_permit_honesty.py` | None |
| Never | Delegatecall proxies, CREATE and CREATE2 factories, account abstraction, Governor, raw calldata | | | Stay listed below |

## Permanent non-goals

The following do not become supported merely because OZ has a module with a similar name:

- Proxies, UUPS/beacons/clones, `delegatecall`, CREATE/CREATE2/CREATE3, arbitrary storage-slot
  derivation, and transient-slot APIs.
- Raw or arbitrary calldata, generic low-level calls, multicall/simulation, ERC-2771 forwarded
  calldata, and callback-driven receiver/hook protocols that are not a closed ABI.
  Bounded ERC-721/1155 receiving hooks (`onERC721Received`, `onERC1155Received`,
  `onERC1155BatchReceived`; `data` at most 32 bytes; batch at most four slots) are in via
  `ReceiverLink`. An ERC-1271 wallet (`isValidSignature` on a `pf` contract) stays out.
  Calling a known hook with a fixed ABI through `OpenCall.callMagic` is in.
- Account abstraction, ERC-4337/7579 execution, paymasters, and EIP-8130 payer flows.
- Governor/timelock/votes systems, unbounded proposal/voter/account relations, and unbounded
  enumerable map/set semantics. Existing hashed-map leaves are fixed-purpose storage primitives;
  they are not a promise to provide arbitrary, enumerable unbounded map APIs.
- ERC-1155 unbounded batches and unbounded event arrays. Typed event frames are bounded: scalar fields plus at most two bounded dynamic-array tails (`BoundedVec` over a closed scalar, declared after every scalar field, at most 63 elements each). Bounded batches of at most four slots (`balanceOfBatch`, `safeBatchTransferFrom` with outbound `onERC1155BatchReceived`) and the bounded `TransferBatch` event are in; interleaved dynamic fields (Governor `ProposalCreated`) stay out.
- Cross-chain bridge/executor protocols, global registries, mainnet endorsement, and bytecode
  refinement proofs.

For the general product boundary and engineering claims, see the
[support matrix](support-matrix.md).
