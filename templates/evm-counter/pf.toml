# Generated by `pf init`. Pinning a published ProofForge git tag is not available yet;
# `pf init` rewrites the Lake path-require to the current checkout.
name = "my-contract"
target = "evm"

[[program]]
name = "Counter"
module = "MyContract.Counter"
