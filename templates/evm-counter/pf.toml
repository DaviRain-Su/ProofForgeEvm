# Generated by `pf init`. The template ships a git-tag require. From a checkout,
# `pf init` rewrites it to a path require against that checkout.
name = "my-contract"
target = "evm"

[[program]]
name = "Counter"
module = "MyContract.Counter"
